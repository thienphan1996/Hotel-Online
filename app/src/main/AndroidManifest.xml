<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.datphongkhachsanonline">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hotel_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login" />
        <activity android:name=".ManHinhChao">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DatPhong"
            android:label="Đặt phòng"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Dialog_GoiMonAn"
            android:theme="@style/NoTitleDialog" />
        <activity
            android:name=".ThanhToan"
            android:label="Thanh toán"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PhongDaDat"
            android:theme="@style/NoTitleDialog" />
        <activity
            android:name=".DatBan"
            android:theme="@style/NoTitleDialog" />
        <activity
            android:name=".ManHinhKhuyenMai"
            android:label="Danh sách khuyến mãi"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".DoiDiem"
            android:theme="@style/NoTitleDialog"></activity>
    </application>

</manifest>